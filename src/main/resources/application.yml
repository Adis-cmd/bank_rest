spring:
  output:
    ansi:
      enabled: ALWAYS
  application:
    name: Bank Cards

jwt:
  secret: BankCardsKey_a8f3j2k9m5n7p1q4r6s2t0u2v4w6x12y5
  lifetime: 30m

  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    change-log: classpath:/db/migration/master.yaml
    enabled: true

server:
  port: 9999
